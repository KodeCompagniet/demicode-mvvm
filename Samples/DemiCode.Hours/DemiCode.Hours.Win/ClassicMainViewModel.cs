using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using DemiCode.Mvvm;

namespace DemiCode.Hours.Win
{
    public class ClassicMainViewModel : ViewModelBase
    {
        public override void Initialize(object model)
        {
            base.Initialize(model);
        }

        public override void RegisterCommandBindings(ICommandBindingRegistry registry)
        {
            base.RegisterCommandBindings(registry);
        }
    }
}
