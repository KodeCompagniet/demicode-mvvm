<Mvvm:UserControlView x:Class="WpfApplication1.EditPersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Mvvm="clr-namespace:DemiCode.Mvvm;assembly=DemiCode.Mvvm" xmlns:WpfApplication1="clr-namespace:WpfApplication1" mc:Ignorable="d" 
                      ViewModelType="{x:Type WpfApplication1:EditPersonViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="Blue" BorderThickness="1" Padding="4" Margin="1" CornerRadius="6">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="13" Text="{Binding FirstName}" />
                <TextBlock FontSize="13" Text=" " />
                <TextBlock FontSize="13" Text="{Binding LastName}" />
            </StackPanel>
            <TextBox FontSize="9" Foreground="Gray" Text="{Binding BirthYear}" />
            <TextBlock Text="{Binding BirthYear}" />
        </StackPanel>
    </Border>
</Mvvm:UserControlView>
