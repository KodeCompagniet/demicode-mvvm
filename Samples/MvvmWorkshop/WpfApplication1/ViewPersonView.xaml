<Mvvm:UserControlView x:Class="WpfApplication1.ViewPersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                      xmlns:Mvvm="clr-namespace:DemiCode.Mvvm;assembly=DemiCode.Mvvm" 
                      mc:Ignorable="d" 
                      d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="Blue" BorderThickness="1" Margin="1" Padding="4" CornerRadius="6">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="13" Text="{Binding FirstName}" />
                <TextBlock FontSize="13" Text=" " />
                <TextBlock FontSize="13" Text="{Binding LastName}" />
            </StackPanel>
            <TextBlock FontSize="9" Foreground="Gray" Text="{Binding BirthYear}" />
        </StackPanel>
    </Border>

</Mvvm:UserControlView>
